import React from 'react';

const CanvasComponent = () => {
  return (
    <canvas
      width="700"
      height="350"
      style={{
        background: 'skyblue',
      }}
    />
  );
};

export default CanvasComponent;
